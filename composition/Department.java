public class Department {
    // shell: no attributes (owned by University in composition)
    // NOTE: Composition component — exclusively owned by a University.
    // Composition implies the Department's lifecycle is tied to the
    // parent; ensure the parent performs removal/cleanup when it is
    // deleted/removed (e.g., parent clears references or runs cleanup
    // code — TODO).
}
